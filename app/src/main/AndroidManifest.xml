<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.citelogbook">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CITELogbook">
        <activity android:name=".attend_content"></activity>
        <activity android:name=".add_sched" />
        <activity android:name=".edit_user" />
        <activity android:name=".leave_content" />
        <activity android:name=".add_user" />
        <activity android:name=".profile_user" />
        <activity android:name=".profile_admin" />
        <activity android:name=".Sched_admin" />
        <activity android:name=".Leave_user" />
        <activity android:name=".dash_user" />
        <activity android:name=".dash_admin" />
        <activity android:name=".Leave_admin" />
        <activity android:name=".Report_admin" />
        <activity android:name=".UserSettings_admin" />
        <activity android:name=".faculty_admin" />
        <activity android:name=".Sched_user" />
        <activity android:name=".home_user" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>